$(document).ready(function(){
  $('.slider').slick({
    autoplay: true,
    autoplaySpeed: 1500,
    infinity: true,
    speed: 1000,
    // adaptiveHeight: true,
    easing: "ease",
    dots:true,
  });
});